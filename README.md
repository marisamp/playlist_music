# 🎧🎷 Music Studio 

แอปพลิเคชันสำหรับแสดงรายการเพลงและเล่นเพลงตัวอย่าง โดยดึงข้อมูลจาก iTunes Search API (Apple Music) โดยใช้ศิลปิน PERSES เป็นโปรเจกต์ตัวอย่างในการพัฒนา

## ✨ Features (ความสามารถของแอป)
- **Song Listing:** Song Listing: แสดงรายการเพลงพร้อมรูปภาพปกอัลบั้ม ชื่อเพลง, และศิลปิน
- **Music Player:** เล่นเพลงตัวอย่าง (Preview) จาก URL โดยมีระบบเช็กสถานะการเล่นแบบ Real-time
- **Interactive Mini Player:** แถบควบคุมด้านล่างที่โชว์เพลงที่เลือกเล่น พร้อมเส้น Progress Bar แสดงความคืบหน้าของเพลง
- **List State Sync:** ไอคอน Play/Pause ในรายการ (List) จะเปลี่ยนตามสถานะการเล่นจริงของเพลงนั้นๆ อัตโนมัติ

## 🛠️ Tech Stack (เทคโนโลยีที่ใช้)
- **Framework:** Flutter 
- **State Management:** GetX (จัดการสถานะข้อมูลและ Logic การเล่นเพลง)
- **Networking:** ดึงข้อมูลผ่าน iTunes Search API โดยใช้ Dio
- **Audio: audioplayers:** สำหรับการจัดการเสียงและ Stream เวลาของเพลง
- **UI Components:** Obx สำหรับการอัปเดต UI 

## 📁 Project Structure

โค้ดหลักจะอยู่ในโฟลเดอร์ `lib` โดยแบ่งสัดส่วนตามหน้าที่ (Pattern):

```text
lib/
├── app/
│   ├── data/            # จัดการข้อมูล (Models, Services)
│   │   ├── models/      # คลาส SongModel และ Song (รับได้ทั้ง String/Map JSON)
│   │   └── services/    # ฟังก์ชันเชื่อมต่อ iTunes API ผ่าน Dio
│   ├── modules/         # แบ่งตามหน้าจอ (Features)
│   │   └── home/        # หน้ารายการเพลงและการเล่นเพลง
│   │       ├── controller/ # จัดการ Play/Pause, Search, และ Scroll Logic
│   │       └── view/       # หน้าจอหลัก (Search Bar + ListView + Mini Player)
│   └── routes/          # จัดการเส้นทางหน้าจอ
├── core/                # ส่วนกลาง (AppColors, AppConstants)
└── main.dart            # จุดเริ่มต้นของแอปพลิเคชัน
```

## 🧠 State Management Logic

ในโปรเจกต์นี้มีการจัดการสถานะของข้อมูล (State) ในหน้า Song List โดยใช้ GetX ดังนี้:

- **Loading State:** แสดง `LoadingAnimationWidget` เมื่อเริ่มดึงข้อมูลจาก API โดยใช้ตัวแปร `.obs` ในการเช็กสถานะ
- **Data State:** เมื่อดึงข้อมูลสำเร็จ จะทำการอัปเดต UI ทันทีผ่าน `Obx` เพื่อแสดงรายการสินค้าที่ได้รับจาก Model
- **Error State:** มีการจัดการกรณีดึงข้อมูลไม่สำเร็จด้วย `try-catch` และแสดง Error Message พร้อมปุ่มให้ผู้ใช้สามารถกดโหลดข้อมูลใหม่ (Retry) ได้
- **Image Handling:** ใช้ `loadingBuilder` และ `errorBuilder` ใน `Image.network` เพื่อจัดการรูปภาพที่กำลังโหลดหรือลิ้งก์เสีย ไม่ให้กระทบต่อการแสดงผลรวมของแอป

## 📸 Screenshots แต่ละหน้าจอ
- หน้า Loading
<img width="460" height="816" alt="splash" src="https://github.com/user-attachments/assets/86ca5dcd-3ee6-49d7-88b1-e2add0490e00" />

- หน้า Playlist Song
<img width="455" height="812" alt="song1" src="https://github.com/user-attachments/assets/44552a6b-6361-4f6c-b3ff-0cd8842eb8ad" />
<img width="452" height="807" alt="song2" src="https://github.com/user-attachments/assets/bb01d3cb-f6ec-4884-82eb-a5a9a4af53b6" />

